{% extends 'admin/base.html.twig' %}

{% block title %}
    Dashboard
{% endblock %}

{% block page_content %}
    <div class="row mt-3">
        <div class="col-lg-12 col-xl-10 mx-auto">
            <div class="row">

                <div class="col-lg-12 correction-no-paddingX mt-20px">
                    <div class="card">
                        <div class="card-body">
                            <div class="h3 card-title">Összes horoszkóp</div>
                        </div>
                        <div class="table-responsive">
                            <table class="table table-hover">
                            {% for horoscope in horoscopes %}
                                <tr>
{#                                    <td class="">#}
{#                                        <strong>{{ horoscope.astrologicalSign.name }} <i>{{ horoscope.type }}</i> | {{ horoscope.date|date('Y-m-d') }} | {{ horoscope.locale }}</strong>#}
{#                                        <br><br>#}
{#                                        {{ horoscope.content|nl2br }}#}
{#                                    </td>#}
{#                                    <td class="">#}
{#                                        <strong>{{ horoscope.astrologicalSign.name }} | {{ horoscope.date|date('Y-m-d') }} | {{ horoscope.locale }}</strong>#}
{#                                        <br><br>#}
{#                                        <i>BASE: {{ horoscope.base.date|date('Y-m-d') }} | {{ horoscope.base.astrologicalSign.name }}</i>#}
{#                                        <br>#}
{#                                        {{ horoscope.base.content|nl2br }}#}
{#                                        {% if horoscope.addendum is defined and horoscope.addendum.content is defined and horoscope.addendum.content is not empty %}#}
{#                                        <br><br>#}
{#                                        <i>ADDENDUM: {{ horoscope.addendum.date|date('Y-m-d') }} | {{ horoscope.addendum.astrologicalSign.name }}</i>#}
{#                                        <br>#}
{#                                        {{ horoscope.addendum.content|nl2br }}#}
{#                                        {% endif %}#}
{#                                    </td>#}

                                    <td class="">
                                        <strong>{{ horoscope.locale }}</strong>
                                        {% for textPublished in horoscope.horoscopeTextsPublished %}
                                            {{ textPublished.publishDate|date('Y-m-d') }} | {{ textPublished.astrologicalSign.name }}
                                        {% endfor %}
                                        <br><br>
                                        <i>BASE:</i>
                                        <br>
                                        {{ horoscope.base|nl2br }}

                                        {% if horoscope.addendum is defined and horoscope.addendum is not empty %}
                                            <br><br>
                                            <i>ADDENDUM:</i>
                                            <br>
                                            {{ horoscope.addendum|nl2br }}
                                        {% endif %}
                                    </td>
                                </tr>
                            {% endfor %}
                            </table>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-lg-12 col-xl-10 mx-auto">
{#            <div class="row">#}
                Dátum formátum: Y-m-d
{#            </div>#}
        </div>
    </div>
{% endblock %}
